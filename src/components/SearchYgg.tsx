"use client"

import MovieList from "./MoviesList"

export default function SearchYgg() {
  return (
    <main className="flex min-h-screen w-full flex-col items-center justify-start">
      <MovieList />
    </main>
  )
}
